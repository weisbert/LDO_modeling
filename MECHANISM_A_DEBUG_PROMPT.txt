Mechanism A（ADE-native in-situ LDO 抽取）— 下一回合 brief。先读：
  - 记忆 ade-backend-1610-1707-fix（含完整 play-by-play）、binpsf-binary-format、
    simkit-mechanism-a-reference、ldo-phase4-production-reality
  - HANDOFF.md 顶部 UPDATE (2026-06-15)
  - 代码：cadence/insitu/{run,adestate,augment,cli}.py、cadence/skill/insitu_run.il、
    cadence/binpsf.py、cadence/insitu/manifests/pmu_top.json

==================== 核心已完成并已 push（origin/target-b-cadence-bringup @ 818b959）====================

整条 in-situ 抽取链在实时 ADE 会话上端到端跑通了。本回合（2026-06-14/15）三个 commit：
  1b9c592  根因修复：ade "卡死" = axlGetRunStatus 读反了（返回 (完成,总数)，不是空闲码；
           老代码等 ==[0,0] 永远等不到）。改成 完成>=总数、且按【单条 history】查
           （会话级聚合改名多了会中毒、还重复计数）。PSF 定位锚到 axlGetResultsLocation
           （老代码 asiGetAnalogRunDir() 参数都传错 → 永远找不到）。不再改 history 名（用
           自动名 Interactive.N，避开 ASSEMBLER-2409 撞名模态框）。
  da7077d  噪声修通 + 干净会话 handle-0 坑：噪声输出模式是 sev 字段 outType（'voltage'|'probe'），
           默认 'probe' 会发 oprobe=<net> → SFE-1997。改 outType='voltage' + p=net,n=gnd! +
           清 oprobe + inType='none' → 网表 noise (VDD0P8_PLL gnd!) noise，spectre 0 errors。
           干净会话 axlGetCurrentHistory 返回 0（SKILL 里 0 是真值！）→ insituCurHist + _cur_hist() 都已防。
  818b959  config 视图保真度：insituBuildConfig 把设计师 Test_PMU/config 镜像到
           Test_PMU_extract/config（继承 viewlist/stoplist/绑定），顶层换成我们 augment 后的
           schematic；dut.extract_view='config'。实测 config 跑出的 14 个量与 schematic 逐位一致（Δ=0）。

实测结果：8 组 ~26s，14/14 contract 数组 → pmu_top_ade.npz → fit 报告（Zout/PSRR/噪声/导纳/电流-PSRR）。
gate-vs-gold FAIL 是【信息性】的（ADE 155 点扫频 vs CLI 金标 51 点）。离线回归（spectre_cli 金标门 +
binpsf 自测）全绿，没碰坏离线路径。

跑法（干净会话上）：cd cadence && python -m insitu run --backend ade --manifest pmu_top --session fnxSession0
  （run_ade 默认 build_first=True；extract_view=config 时会先 insituBuildConfig 再绑 config 视图）

==================== 本回合可做（全是小项 / 都不阻塞）====================

[5] 定向 save：现在 save=allpub（binpsf 能读，工作正常）。axlAddOutputSignal 只存需要的端口 = 优化，
    省 PSF 体积 / IO。非必须。
[6] 可读的 Maestro history 名：现在驱动用自动名 Interactive.N（为了躲开 re-run 撞名弹的 ASSEMBLER-2409
    模态框）。要可读名就先 delete-then-rename（查 axlDeleteHistory / 同名先删再 axlSetHistoryName），
    一定在干净会话上做、且做成幂等。
[G] GUI Tab-0 实测：GUI #4 的进度/取消之前只在【坏后端】上验过（还验过超时中止）。现在后端能跑了，
    用 GUI 跑一次 ade，确认进度条走、Cancel 干净中止、产出 npz。离屏 selftest 仍绿。

==================== 硬约束 / 坑（仍然适用）====================
- 会话卫生：~6 次 run + 改名会把会话搞退化（run 3s→197s、聚合状态中毒、撞名弹模态框）。
  Session→Reset 能完全恢复（聚合回到 [0,0]）。要跑就在干净会话上、别反复 augment/开关 cellview。
- 探活：cd cadence && python3 -c "from skillbridge import Workspace as W; print(W.open()['plus'](2,3))" 要秒回 5。
- SKILL 走 virtuoso-skill 协议：grep ~/.claude/skills/virtuoso-skill/assets/function_index.tsv → 读 PDF
  （/home/yusheng/cadence_work/Test/workarea/SKILL_file/...）→ 带出处，绝不凭记忆写签名。
- skillbridge：模态框冻通道；杀客户端不能取消已在 Virtuoso 里跑的调用；符号用 from skillbridge import Symbol。
  axl* 的 "Cannot find setup database entry for handle 0" 这类错 errset 抓不住 → Python 侧也要 try/except。
- 不改设计师脊柱：只动副本 insitu_extract / Test_PMU_extract。保 npz firewall。
- 实时会话事实：session=fnxSession0，sdb=1001；设计师 test="Test"（cell Test_PMU，view config）；
  我们的 test="insitu_extract"（cell Test_PMU_extract，现在 view=config）。
  设计师 OP：flo=5G/VDD=3/VDD1P0=1.05。ac 字段 start/stop/dec；noise 用 outType=voltage + p/n。
- 结果路径：axlGetResultsLocation(sdb) = ~/simulation/sim_yusheng/Test_PMU/maestro/results/maestro；
  单次 run 落在 <root>/<hist>/1/insitu_extract/psf/{ac.ac,noise.noise}。
