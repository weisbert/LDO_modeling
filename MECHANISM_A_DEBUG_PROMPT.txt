Mechanism A（ADE-native in-situ LDO 抽取）— 下一回合 brief。先读：
  - 记忆 ade-backend-1610-1707-fix、binpsf-binary-format、ldo-phase4-production-reality、
    simkit-mechanism-a-reference
  - HANDOFF.md 顶部 UPDATE (2026-06-14c)
  - 代码：cadence/binpsf.py、cadence/insitu/{adestate,run,cli,augment}.py、gui/ldo_modeler.py

==================== 已完成（已 commit，未 push）====================

【1610/1707】设计师 ADE 状态继承（adestate.py，经 axlGetToolSession→asiGetSession 桥）已坐实修通。
【ade 数据读取】binpsf.py 二进制 PSF 读取器已做好并实测：psf.read_psf 按字节自动分派 ascii/binary。
  AC（51点×34迹）和 noise（141点，含 per-instance STRUCT 迹，标量 out 精确）都用真实 spectre
  -format psfascii 交叉验证通过（AC 残差 3.9e-6 = psfascii 6位有效数字截断，二进制反而是全精度）。
  importmp 的 Zout/noise 推导在真实二进制 PSF 上验证通过。test_binpsf.py + cadence/testdata/binpsf 夹具。
【GUI #4】run_ade 加了 progress(frac,msg)+cancel()；Tab0 进度条+Cancel；超时不再对未 idle 的 run
  做破坏性 insituRename（否则 ASSEMBLER-2423），干净中止。
【GUI #1】Tab0 Edit…/New… 内置 manifest JSON 编辑器（Validate 用同一套 manifest.validate + 测量矩阵预览，
  Save/Save As/模板新建）。
都跑通：test_binpsf(AC+noise)、spectre_cli gate vs gold 0.00e+00 无回归、离屏 GUI selftest 绿。
commits：01d8478 / 1fbf108 / 180287a / 06a0e38（+ 上一回合 3b7f000/69f9beb/dc7ec27）。

==================== 本回合要干的 ====================

[4]（周一，必须用实时会话）config-view 保真度——最后一个真问题。
   现状：augment.py 的 insituCopyTB(tb_lib, tb_cell, ext_cell, tb_lib, "schematic") 只复制 TB 的
   schematic 视图，extract test 跑 ext_cell/schematic。但设计师的 "Test" 用 Test_PMU/config，
   config 可能给 DUT/子块绑定了 extracted/寄生 视图。
   风险：真 PMU 若 config 绑了寄生视图，schematic 副本会丢掉 RC 寄生 → 抽出的 Zout/PSRR/noise
   是无寄生的 schematic 级，跟设计师 config 看到的不一致。stand-in 全 schematic 所以现在看不出来。
   先做：用实时会话查 Test_PMU/config 到底绑了什么视图（hiViewConfig / 看 config 的 view list；
   走 virtuoso-skill 协议查 SKILL 签名）。若绑了 layout/extracted 视图，则把 extract test 建成一个
   CONFIG，顶层换成我们 augment 后的 ext schematic，底下继承设计师的 view-binding 规则。
   坑：skillbridge 会被 Virtuoso 模态框冻住整条通道，遇冻关弹框或重启 Cadence；config-view 激活过
   旧/坏会话也会冻——只在当前会话上操作。

[5]（小，非阻塞）定向 save：现在 save=allpub（binpsf 能正常读，只是多存数据）。
   axlAddOutputSignal（adexl p.243）做定向保存集是优化项。

之后：真 PMU bring-up（换 manifest → 用新的 GUI 编辑器改/新建 → Build&Run → 出 npz → fit）。

==================== 硬约束 / 坑 ====================
- SKILL 走 virtuoso-skill 协议：grep ~/.claude/skills/virtuoso-skill/assets/function_index.tsv
  → 读 PDF（/home/yusheng/cadence_work/Test/workarea/SKILL_file/...）→ 带出处，绝不凭记忆写签名。
- skillbridge 模态框冻通道；杀客户端不能取消已在 Virtuoso 里跑的调用；遇冻关弹框或重启 Cadence。
- 不改设计师脊柱：只动副本 insitu_extract / Test_PMU_extract。保 npz firewall。
- skillbridge 符号用 from skillbridge import Symbol（Symbol('ac')->'ac）。
- 实时会话事实：session=fnxSession0，sdb=1001；设计师 test 名 "Test"（manifest dut.ade_src_test 已写）；
  设计师 OP flo=5G/VDD=3/VDD1P0=1.05；spectre ac 字段 start/stop/dec，noise 多 oprobe/iprobe。
- 验证二进制 PSF 读取：把同一 input.scs 用 spectre -64 -format psfascii 再跑出 ascii 参照对比；
  二进制-vs-二进制是逐位一致；psfascii 只有 ~6 位有效数字。
