# Runtime deps for the OFFLINE GUI modeler (analytic-only path; NO simulator, NO scikit-rf).
# These are the DIRECT pins; transitive deps (pillow/contourpy/kiwisolver/fonttools/cycler/
# python-dateutil/pyparsing/packaging) are resolved + audited by package.py and frozen into
# requirements.lock. Versions chosen to still ship manylinux_2_17 (glibc-2.17 / CentOS7) wheels
# for cp311 x86_64 -- VERIFY with deploy/audit_wheels.py on yellow-zone day 1; downpin on any
# manylinux_2_28 hit (the #1 airgap risk -- see GUI_DEPLOY_PLAN.md §4).
numpy==1.26.4
scipy==1.15.3          ; python_version >= "3.11"   # need >=1.15 for scipy.interpolate.AAA
matplotlib==3.9.4
PyQt5==5.15.10
PyQt5-Qt5==5.15.2      # KEY: 5.15.2 is manylinux2014; 5.15.11+ may need glibc 2.28
PyQt5-sip==12.15.0

# skillbridge: the LIVE-Virtuoso bridge (Mode A pin-resolve / ADE engine / create-cell). Pure
# Python, py3-none-any, no glibc concern. It does NOTHING offline -- every import of it in the
# app is function-local, so bundling it never affects the analytic-only path. PIN to the version
# matching the CIW SKILL server (1.8.0 here); a client/server version skew breaks the handshake.
# To actually connect you still start the server in CIW (pyStartServer) -> GUI 'Check' -> green.
skillbridge==1.8.0
