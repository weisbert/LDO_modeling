# archive — history (not loaded by default)

Superseded handoffs and the old append-only `HANDOFF.md` journal. Kept for deep-dive / provenance.
Their durable content has been distilled into `docs/reference/{DATA,TOOL_FACTS,METHODOLOGY}.md` and
`docs/BACKLOG.md`. Don't add to these; don't read them by default. Current state lives in `STATUS.md`.
